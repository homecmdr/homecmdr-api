meta {
  name: Delete Room
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rooms/:id
}

params:path {
  id: {{roomId}}
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
