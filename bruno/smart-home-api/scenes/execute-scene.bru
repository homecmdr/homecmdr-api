meta {
  name: Execute Scene
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/scenes/:id/execute
}

params:path {
  id: {{sceneId}}
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
