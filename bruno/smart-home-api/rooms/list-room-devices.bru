meta {
  name: List Room Devices
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/rooms/:id/devices
}

params:path {
  id: {{roomId}}
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
