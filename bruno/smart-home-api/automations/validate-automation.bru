meta {
  name: Validate Automation
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/automations/:id/validate
}

params:path {
  id: {{automationId}}
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
