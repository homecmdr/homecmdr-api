// This crate previously forced compile-time adapter registration via
// `inventory::submit!`.  That mechanism has been removed in favour of the
// WASM plugin system (crates/plugin-host).  This crate is retained as a
// placeholder during the Phase 4 cleanup and will be removed from the
// workspace in a follow-up commit.
